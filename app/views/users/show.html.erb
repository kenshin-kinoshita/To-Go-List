<h1><%= @user.name %> のプロフィール編集画面</h1>

<table class="table table-bordered">
  <tr>
    <th>ニックネーム</th>
    <td><%= @user.name %></td>
  </tr>
  <tr>
    <th>メールアドレス</th>
    <td><%= @user.email %></td>
  </tr>
</table>

<%= link_to 'ユーザ情報を編集する', edit_user_path(@user), class: 'btn btn-light' %>